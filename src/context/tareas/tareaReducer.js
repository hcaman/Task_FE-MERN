import {
  FORMULARIO_PROYECTO,
  OBTENER_PROYECTO,
  AGREGAR_PROYECTO,
  ELIMINAR_PROYECTO,
  PROYECTO_ACTUAL,
  VALIDAR_FORM,
} from '../../types';

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
